kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: k8s-watcher-test
nodes:
  - role: control-plane
    extraPortMappings:
      # For webhook testing - expose NodePort service
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
